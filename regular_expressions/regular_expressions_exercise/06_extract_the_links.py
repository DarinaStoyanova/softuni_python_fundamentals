import re
text = input()
while text != " ":
    pattern = r"\s((www\.[a-zA-Z0-9]+[a-zA-Z0-9\-]*)(\.[a-z]+)+)"
    matches = re.sea

    text = input()
    # pattern = r"\s((www\.[a-zA-Z0-9]+[a-zA-Z0-9\-]*)(\.[a-z]+)+)"
    # matches = re.findall(pattern,text)
    # for match in matches:
    #     print(match[0])
    # text = input()